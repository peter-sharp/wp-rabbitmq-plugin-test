{
  "name": "realtimeapp",
  "version": "1.0.0",
  "description": "Listens to rabbit mq, updates page in realtime",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "amqplib": "^0.5.2",
    "socket.io": "^2.0.4"
  }
}
